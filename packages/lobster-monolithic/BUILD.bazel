load("@rules_python//python:defs.bzl", "py_binary")
load("//:requirements.bzl", "requirement")

py_binary(
    name = "package_monolithic",
    srcs = ["package_monolithic.py"],
    visibility = ["//visibility:public"],
    deps = [
        requirement("build"),
    ],
)

#bazel run //packages:package_monolithic
