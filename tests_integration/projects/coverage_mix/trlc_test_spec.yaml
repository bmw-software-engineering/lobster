inputs:
  - example.rsl
  - req_a.trlc
  - req_b.trlc
  - test_spec.trlc

conversion-rules:
  - package: coverage_mix
    record-type: Test_Specification
    namespace: req
    description-fields: text
    justification-up-fields: just_up
    justification-down-fields: just_down
    tags: [verifies]
