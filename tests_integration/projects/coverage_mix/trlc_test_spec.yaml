inputs :
  - example.rsl
  - req_a.trlc
  - req_b.trlc
  - test_spec.trlc

trlc_config : |
  coverage_mix.Test_Specification {
    description = text
    tags "req"  = verifies
    just_up     = just_up
    just_down   = just_down
  }
