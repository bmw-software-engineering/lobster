load("@rules_python//python:defs.bzl", "py_binary")
load("//:requirements.bzl", "requirement")

py_binary(
    name = "package_core",
    srcs = ["package_core.py"],
    visibility = ["//visibility:public"],
    deps = [
        requirement("build"),
    ],
)

#bazel run //packages:package_core
